name: Primeiro Workflow Linuxtips

on:
    push:
        branches:
            - main

jobs:
    giropops:
        runs-on: ubuntu-latest
        steps:
            - name: Confere versao do ubuntu
              run: cat /etc/os-release
            
            - name: Exibir Mensagem
              run: echo "Estou fazendo o curso Github Actions da Linuxtips"

            - name: Exibir IP
              run: ip a
